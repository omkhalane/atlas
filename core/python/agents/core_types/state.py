"""
PHASE 3B STUB — DO NOT IMPLEMENT HERE.

The authoritative agent state and task state management lives in:
    packages/runtime/kernel/state_manager.py  (StateManager)
    packages/runtime/kernel/task.py           (ExecutionTask, TaskStatus)
    packages/runtime/kernel/task_manager.py   (TaskManager)

This file is intentionally empty. It is a Phase 3B placeholder created
during the migration to establish the core package structure.

If a core-level state abstraction is ever needed, it must be expressed
as a Protocol/ABC contract in core/python/contracts/, NOT as a concrete
StateManager implementation here.

See Phase 5 reconciliation: phase5_runtime_integration_reconciliation.md
"""
