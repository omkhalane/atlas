# @atlas/config

Configuration schemas, workspace settings defaults, and environment parsing for Atlas.

## Overview

Central repository of JSON schemas, default settings files (`config.json`), and validation helpers for Atlas platform components.
